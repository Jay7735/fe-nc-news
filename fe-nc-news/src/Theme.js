import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: '#bdbdbd', // Replace with your desired primary color
      },
      secondary: {
        main: '#9e9e9e'
        , // Replace with your desired secondary color
      },
    },
  });

  export default theme